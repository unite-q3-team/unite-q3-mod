// System
sv_pmove_fixed         // фиксированное перемещение игроков (true/false)
sv_pmove_msec          // интервал обновления физики в мс (при sv_pmove_fixed)

// Game modes/modificators
g_instagib             // Истагиб. 1 - вкл, только рейл, 2 - вкл, рейл + перчатка
g_railJump             // Рейл-джамп

g_vampire              // Вампиризм (вовзрат ХП от в зависимости от нанесённого урона), в %, где 1 - 100%
g_vampireMaxHealth     // Максимальное хп при вампиризме

// Player spawn
g_selfdamage           // наносит ли урон себе
g_startWeapon          // стартовое выбранное оружие (если разрешено)
g_startWeapons         // битовая маска стартовых оружий, где 2 - пулемёт, 4 - шотган и т.д.

// items

disable_%              // возможность отключать выбранные предметы, например disable_weapon_rocketlauncher 1 - отключает спаун rocketlauncher (дефолт bq3a фича)
//g_items                // Предметы на карте (дроп/спаун). 0/1
//g_spawnItems           // Выбор предметов для спауна. 1 - здоровье, 2 - броня, 4 - оружия, 8 - патроны, 16 - усилители (power ups)

g_spawn_weapons          // Спаун оружий и патронов к ним. 4 - машинган, 8 - шотган и т.д. (надо фикс)   

// Start ammo
g_mg_start_ammo         // количество патронов к пулемёту
g_sg_start_ammo         // патроны для дробовика
g_gl_start_ammo         // гранаты
g_rl_start_ammo         // ракеты
g_lg_start_ammo         // заряд молнии
g_rg_start_ammo         // патроны к рельсотрону
g_pg_start_ammo         // плазма
g_bfg_start_ammo        // BFG
//Ignore mission pack
g_start_ammo_nailgun       // гвоздомёт (MISSIONPACK)
g_start_ammo_proxlauncher  // прокси-мины (MISSIONPACK)
g_start_ammo_chaingun      // пулемёт (MISSIONPACK)

// Start stats
g_start_health         // здоровье при спавне
g_start_armor          // броня при спавне

// Weapons
g_gauntlet_damage      // урон перчатки
g_mg_damage            // урон пулемёта
g_mg_damageTeam        // урон пулемёта в командных режимах игры 
g_sg_damage            // урон дробовика
g_gl_damage            // урон гранатомёта
g_rl_damage            // урон ракетницы
g_lg_damage            // урон молнии
g_rg_damage            // урон рельсотрона
g_pg_damage            // урон плазмы
g_bfg_damage           // урон BFG
g_damageThroughWalls   // Урон от взрывов через пол/стены

g_rl_projectileSpeed   // Скорость снаряда ракетницы 
g_pg_projectileSpeed   // Скорость снаряда плазмы 
g_bfg_projectileSpeed  // Скорость снаряда BFG  

g_gauntlet_fireRatio    // Скорострельность перчатки
g_lg_fireRatio          // Скорострельность молнии
g_sg_fireRatio          // Скорострельность дробовика  
g_mg_fireRatio          // Скорострельность пулемёта  
g_gl_fireRatio          // Скорострельность гранатомёта  
g_rl_fireRatio          // Скорострельность ракетницы  
g_pg_fireRatio          // Скорострельность плазмы  
g_rg_fireRatio          // Скорострельность рельсотрона  
g_bfg_fireRatio         // Скорострельность BFG  

//freeze
g_thawTime              // Требуемое время на разморозку, мс
g_thawTimeAutoRevive    // Требуемое время на автто-разморозку, мс
g_thawTimeAuto_lava     // Требуемое время на автто-разморозку в лаве/кислоте, мс
g_thawTimeAuto_bounds   // Требуемое время на автто-разморозку за преедлами карты, мс
g_thawTimeAuto_tp       // Требуемое время на автто-разморозку в телепорте, мс
g_thawRadius            // Радиус разморозки

//xq3e
g_x_drawDamage          // Показывать урон
g_x_unfreezeFoe         // Показывать клепсидру разморозки (хз будет ли воркать правильно)
g_x_drawHitbox          // Показывать хитбокс